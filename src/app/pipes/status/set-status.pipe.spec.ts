import { SetStatusPipe } from './set-status.pipe';

describe('SetStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new SetStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
